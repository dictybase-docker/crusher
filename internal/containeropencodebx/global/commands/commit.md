---
description: Create a conventional commit from the current changes
---

Stage relevant changes, infer the conventional commit type and scope from the
diff, and write a concise message that explains *why* the change exists rather
than listing files. Run `git commit` with the generated message.
